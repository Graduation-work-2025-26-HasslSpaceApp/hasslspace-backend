services:
  postgres:
    image: postgres
    container_name: hassl_space_db
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=hassl_space_db