apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.name }}-config
data:
  DEV_ENV_HOST_KAFKA: {{ .Values.env.KAFKA_HOST | quote }}
  DEV_ENV_PORT_KAFKA: {{ .Values.env.KAFKA_PORT | quote }}

  DEV_ENV_SERVER_PORT: "{{ .Values.service.port }}"
